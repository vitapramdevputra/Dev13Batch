public class EmployeePurchaseOrder implements PurchaseOrder{
    //interface ENFORCES to implement methods which are declared in interface.

    //we MUST define the method which is declared in interface.
   public double discount(){
    return 0.10;
   }
}