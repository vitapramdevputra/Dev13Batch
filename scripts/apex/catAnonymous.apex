//normal world.

//1. if our class has no consturctor defined. 
//we can create new instance with empty brackets.
cat sfcat = new Cat();
sfcat.name = 'tekir';
sfcat.color = 'black';
system.debug(sfcat);

cat c2 = new cat('fluffykins','white');
system.debug(c2);